---
import Layout from '~/layouts/PageLayout.astro';
import GrowthHome from '~/components/adelie/GrowthHome.astro';

const metadata = {
  title: 'Adelie｜为增长而建的网站、SEO 与 Google Ads',
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}>
  <GrowthHome />
</Layout>
